name: Deploy to VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: ${{ secrets.SSH_KNOWN_HOSTS }}

      - name: Deploy via PM2 on Remote VM
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} '
            set -e

            # Переходим в нужную папку
            mkdir -p my-registry

            # Если ещё не клонировали — клонируем через SSH
            if [ ! -d .git ]; then
              echo "🔄 Клонируем репозиторий через SSH..."
              git clone git@github.com:${{ github.repository }} . --branch main --depth 1
            else
              echo "🔄 Обновляем код..."
              git reset --hard
              git pull
            fi

            echo "📥 Устанавливаем зависимости"
            npm install -g pm2
            npm install -g verdaccio

            echo "🔄 Останавливаем PM2..."
            pm2 stop all || echo "⚠️ Нет запущенных процессов PM2"
            pm2 delete all || echo "⚠️ Нет приложений для удаления"

            pm2 start verdaccio -- --listen 0.0.0.0:4001

            echo "✅ PM2 перезапущен успешно"
            pm2 list

            echo "🔄 Настраиваем nginx..."
            
            # Копируем конфиг (предполагаем, что он лежит в репо)
            sudo cp nginx/verdaccio.conf /etc/nginx/sites-available/verdaccio
            
            # Создаём симлинку, если её нет
            if [ ! -L /etc/nginx/sites-enabled/verdaccio ]; then
              echo "🔗 Создаём симлинку..."
              sudo ln -s /etc/nginx/sites-available/verdaccio /etc/nginx/sites-enabled/verdaccio
            else
              echo "✅ Симлинка уже существует"
            fi
            
            # Проверяем и перезагружаем nginx
            echo "🔍 Проверяем конфиг nginx..."
            sudo nginx -t
            
            echo "🔄 Перезагружаем nginx..."
            sudo systemctl reload nginx

            echo "✅ Nginx перезапущен успешно"
          '

      - name: Report Success
        if: success()
        run: echo "✅ Deploy is successfull!"

      - name: Report Failure
        if: failure()
        run: echo "❌ Fail to deploy on VM!"
